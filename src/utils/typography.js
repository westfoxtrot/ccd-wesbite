import Typography from "typography";
import Theme from "typography-theme-moraga";
Theme.headerColor = "#fbfef9"
Theme.bodyColor = "#e1e4df"
const typography = new Typography(Theme);

export default typography;
